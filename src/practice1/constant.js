const ADDRESS = "https://console.amazonaws.cn";
const PORT = 4455;
const PATH = "/api";
const REGION = "cn-north-1";

const aws = {
  address: ADDRESS,
  port: PORT,
  path: PATH,
  region: REGION,
};

export default aws;
